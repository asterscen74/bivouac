// REST API & pg_tileserv
// var { api_url } = require("./settings-server");

// Languages
const defaultLanguage = "fr";

const config = {
    defaultLanguage: defaultLanguage,
};


// module.exports = {
//     // api_url,
//     config,
// };



const settings = {
    config,
}

export default settings;
